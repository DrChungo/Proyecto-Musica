import { Component } from '@angular/core';

import { SongList } from '../../components/song-list/song-list';

@Component({
  selector: 'app-guardadas',
  imports: [SongList],
  templateUrl: './guardadas.html',
  styleUrl: './guardadas.css',
})
export class Guardadas {

}
